# Backlog

A place to drop bugs and feature requests as you find/think of them, at any pace. I check this file when we start a work session and pick items up — no need to wait for me to be "free" to write something down.

**How to use it:**
- Add a new line under the right section, in plain language — a screenshot description, a repro step, a half-formed idea. No need to format it carefully.
- I'll investigate, ask a clarifying question inline (right here in the file, or in chat) if something's ambiguous, and check items off (`- [x]`) once shipped, usually referencing the CLAUDE.md sprint entry that covers it.
- Nothing here is a commitment or a queue order — I'll use judgment on sequencing, and may batch related items together.

## Bugs

- Studio scene is not same as what was authored in builder: `places placeholder art` and `background transparency` halves of the original report — still open, pending concrete reproduction. (The `places shapes` half shipped, see Done below.)

## Feature Requests

- We need a tutorial or a starting instruction for the kids first visit. what to do , how to do , where to do, when to do.
- Printable story writing pages, color sheets , sketches. various book sizes.
- **Voice atmosphere — an authored soundtrack for a page, not just one narration.** A page can hold several voice notes; the author should be able to decide which ones play, in what order, and at what time, either alongside the page's narration or instead of it. That authored arrangement is what a Reel export should film — single page or multiple pages. Whenever a voice note plays, its 🔊 badge should animate in the reel itself, so a viewer can see which voice is speaking. (Today: Reel's audio track is the per-page narration only; the badges render as static art and never sound. Ship 3's own scope line — noted there as a clean extension of the same compose pipeline, not a gap.) Design questions worth settling before building: does a note play at an absolute time on the page's timeline or in a sequence relative to the others; does the page's hold time stretch to fit the whole arrangement; and can a note deliberately overlap the narration or only sit between beats.

## Done

- [x] Magic Publish — Sprint M9 (Launch Readiness) — CLAUDE.md: "Magic Publish — Sprint M9: Launch Readiness." A review sprint, audited rather than assumed: four findings, all measured. The video card and the printable Strip said the brand two different ways and in two different faces; the card's `Georgia` stack is not self-hosted, so on this machine a deliberately bogus family measured identically to it — universal fallback; the Strip was genuinely drawing in a fallback font, since nothing else on the page ever asks for Nunito; and the card's companion sat at 31% of frame width in a barely-lit dark rectangle. MagicStrip now owns one brand line and one typeface for both halves, the font is loaded up front and awaited before the one frame that carries the brand, and the card is composed rather than stacked. This completes Magic Publish M1–M9.
- [x] Magic Publish — Sprint M8 (Performance & QA) — CLAUDE.md: "Magic Publish — Sprint M8: Performance & QA." Measured first: a 12-page reveal cost 1.1 GB and filmed for 74 seconds. A story-wide budget caps both, and makes a long book's reveal brisker rather than shorter — no page is ever dropped.
- [x] Magic Publish — Sprint M7 (Audio & Polish) — CLAUDE.md: "Magic Publish — Sprint M7: Audio & Polish." The reveal gains sound and an ending. The audio tier is chosen once per STORY, not per page: if any page carries the child's own narration the whole reel runs on it with no ambient bed underneath, so a quiet voice is never competed with; otherwise one Foundation loop plays low under the whole reel; otherwise real silence. A narration attaches to its page's FIRST frame so the voice runs under the whole assembly, and any shortfall lands on the LAST frame rather than stretching the reveal's own pacing. Every reel now closes on a 2.5s card carrying the story's name, "✨ Created in VihuPlanet", and — for a Creator — their own bonded Story Companion, contain-fit and never cropped; a Traveller simply gets the card without one.
- [x] Magic Publish — Sprint M6 (Publish Celebration) — CLAUDE.md: "Magic Publish — Sprint M6: Publish Celebration." Publishing stops being an export. The destination fork is gone from the default path: pressing the Almost Ready button publishes a bundle (Book + Magic Creation + Magic Strip, with the cover already rendered live) behind a real checklist that ticks each task from "✨ Preparing Story" to "✓ Book Ready", and Celebration opens with the Magic Creation already playing rather than a download button. Every other format is still one tap away under "Other formats", and every artifact is still downloadable.
- [x] Magic Publish — Sprint M5 (Magic Strip) — CLAUDE.md: "Magic Publish — Sprint M5: Magic Strip." A single still picture of how a page was made, laid out as film — sprocket holes, a thin dark gutter, small numerals, and the story's own name beside the brand in a footer bar inside the strip's chrome, never on the artwork. Two layouts (a wide Strip for sharing, a 2-column Grid for printing). New `js/magicStrip.js` is a compositor, not a renderer: it never touches SlideRenderer, only arranges bitmaps the shared render path already produced. One frame per BEAT rather than per stage, so the Words cell carries its finished sentence rather than "Once", and a short final grid row is centred rather than padded with duplicates.
- [x] Magic Publish — Sprint M4 (Magic Animation) — CLAUDE.md: "Magic Publish — Sprint M4: Magic Animation." The reveal finally moves: backgrounds wash in, arrivals fade and rise, and words draw one at a time. Consecutive reveal frames are pixel-identical except for what just arrived, so a plain crossfade already reads as "only the new thing appeared" — no arrival-layer canvases, no undoing M3's memory cleanup. Word-by-word lives in `magicReveal.js` (a crossfade cannot produce the words in between), the composer gains `wash`/`rise`/`draw` beside the existing page turn, and Story Reel's own transition is untouched.
- [x] Magic Publish — Sprint M3 (Frame Rendering) — CLAUDE.md: "Magic Publish — Sprint M3: Frame Rendering." Every reveal stage now renders to its own bitmap through `_renderSlideInto` — the same shared path Book, Carousel and Reel already use, no second renderer — with the finished frame reusing the pipeline's own canvas and every off-screen one tagged and deterministically released on both the success and failure paths. `transition:'none'` until M4 owns the real reveal language.
- [x] Magic Publish — Sprint M2 (Layer Decomposition Engine) — CLAUDE.md: "Magic Publish — Sprint M2: Layer Decomposition Engine." `revealStages(slide)` now derives real `Blank → World → Artwork → Decorations → Text → Finished` stages from the finished page alone, collapsing any beat with nothing to bring, never mutating the slide it is handed, and always ending on the caller's own object.
- [x] Magic Publish — Sprint M1 (Foundation) — CLAUDE.md: "Magic Publish — Sprint M1: Foundation." A new Magic Creation publish destination plus `js/magicReveal.js`, wired end to end through the real Publish pipeline with no UI changes, no renderer changes and no animation yet. `revealStages(slide)` derives stages from the finished page (no history, no timeline) and never mutates the slide; M1 returns the single Finished stage, so the destination produces a real, deliberately non-magical video while M2 fills in the decomposition behind the same signature.
- [x] I like the voice icon, but it's overpowering with backfill — let the creator choose from 3 different voice note icons — CLAUDE.md: "Three Voice Note Badges — and None of Them Is a Solid Backfill." Speaker (the glyph on a soft halo, no disc — the new default), Bubble (a drawn speech bubble, thin coral outline over a faint tint), and Mic (a 🎤 in a thin coral ring). Catalogued once in StickerLibrary.VOICE_ICONS; the picker sits beside Play in the Refine panel and the Object Strip card follows the chosen glyph. An already-placed note picks up the lighter default rather than keeping the heavy disc.
- [x] We can combine spin and move panel into one — CLAUDE.md: "Move and Spin Become One Widget." The 8 direction arrows now ring a Spin disc, with the reset knob sitting on the disc's own hub — one row, one shared label carrying the live degree readout, one two-line legend. Geometry is percentage-based so the widget scales with whatever gutter room the Selection Action Strip finds. A moveable-but-not-rotatable object gets the ring with the hub in its own centre and no disc at all — Guardrails unchanged.
- [x] Bug regarding voice object — there is no way to place it on slide — CLAUDE.md: "Voice Notes Can Finally Be Placed."
- [x] Whenever there is a moveable flag true with any object, give the 8-direction mover like Builder's, along with a 360-degree rotation spinner — CLAUDE.md: "Move Pad + Spin Dial for Every Moveable Object."
- [x] Connect with one specific folder of either google drive or google photos so that kids can pick their photos from there and parents can upload pics there — CLAUDE.md: "Family Photos — Shared Google Photos Albums in Studio (Phases 1-4)." Shipped as Option 1 (public shared Google Photos album + server-side scrape): a parent shares one or more PUBLIC albums (mobile photos sync to Google Photos, matching the real family workflow); the `family-album` Edge Function scrapes the album's photo list and proxies only the actually-picked image's bytes (browsing thumbnails load direct from lh3 for free — the owner's own live go/no-go verdict, GO proxy); album links are stored per-kid in the new `family_albums` table, fully modifiable via the parent-facing Manage Albums panel; the kid picks alongside the ordinary local picker, never replacing it; Creator-only — a Traveller sees a companion-framed nudge toward their first Publish instead. NOTE: supabase/schema.sql must be re-run once in the Supabase SQL editor (idempotent) to create the table + activate a cross-owner RLS fix this work also surfaced.
- [x] Restore loses orientation (landscape→portrait) — CLAUDE.md: "BACKLOG.md — Restore Loses Orientation."
- [x] Performance with 20+ objects on Scene — CLAUDE.md: "BACKLOG.md — Performance with 20+ Objects on Scene."
- [x] For all shapes, choose either solid colors or manually color using brush strokes (Doodle-style) — CLAUDE.md: "BACKLOG.md — Shapes: 'Solid Fill' vs. 'Paint Inside' Toggle."
- [x] Fill style and alignment buttons don't visually update on click (effect applies, button state frozen) — CLAUDE.md: "BACKLOG.md — Fix Single-Select Icon-Row Buttons Not Visually Updating on Click."
- [x] Default state of "Add Something" in the right pane should be open — CLAUDE.md: "BACKLOG.md — Add Something Default-Open Right Pane."
- [x] Add image option also in background — CLAUDE.md: "BACKLOG.md — Add Image Option to Page Background (root Studio only)."
- [x] Doodle drawn vs doodle shown has differences in line stroke thickness and space covered; also leakage from doodle sketch area to outside area, same with shape when painted — CLAUDE.md: "BACKLOG.md — Doodle/Shape Fidelity: Real-Render Leakage Clip + Pad Aspect-Ratio Correction (root Studio only)."
- [x] Color change swatch should be made available where ever colors are used. a standard rainbow color circle should be used for color picker. Doodle area does not have color picker — CLAUDE.md: "BACKLOG.md — Bugs 1, 4, 6 Shipped Together."
- [x] In shapes for all letters add lowercase also — CLAUDE.md: "BACKLOG.md — Bugs 1, 4, 6 Shipped Together."
- [x] Color and Image can co-exist in background. color is always behind the image not in front. Rename Background color section to Background. The image uploaded need to pass through image studio and also need to support all options which other images objects have got — CLAUDE.md: "BACKLOG.md — Bugs 1, 4, 6 Shipped Together."
- [x] +Experience button needs to be in scene stack also — CLAUDE.md: "BACKLOG.md Batch 1 — Bugs E + D."
- [x] Graphics within experience in builder do not rotate on moving the rotation slider — CLAUDE.md: "BACKLOG.md Batch 1 — Bugs E + D."
- [x] For texts allow these curves — CLAUDE.md: "BACKLOG.md — Batch 2 (Bug G): Curved Text End to End..."
- [x] Companion does not always load in studio it just shows blank circle — Resolved by real Quill pose art uploaded (Companion Engine's existing graceful-degradation onerror-fallback correctly handles a missing pose file already; the blank circle only appeared because Quill's own pose files hadn't yet been supplied).
- [x] Add image option in frame styles in builder — CLAUDE.md: "Bug 4 — Image option in Frame Styles (root Studio): a genuine rotation-aware cover-fit fix for all three image-cover call sites in renderer/slideRenderer.js's Frame-fill pipeline" and preceding "Image-Typed Frame Variations" sprints. Frame Style now accepts a real image option (frameOrnamentImage + frameOrnamentImageRotation), mat and border also support image via existing frameImage; every image-cover call site is rotation-aware.
- [x] Allow builder to add more places to the scene — already fully implemented by the earlier "Multiple Artwork Places Per Page" sprint. Every Scene has an "+ Add a Place" button in both the Scene Stack sidebar and the Place activity panel; addHolder assigns unique ids and distinct positions; convergeScene compiles every Place with placeRects (position/size/shape/frame/Guardrails); both engines render every Place independently. Verified end to end via a real Playwright walkthrough (`bug551_uicheck.js`, 9/9): a 1-Place Scene grows to 2, then 3 Places on real button clicks, with correct names "Place, Place 2, Place 3".
- [x] Give rotation capability to places in builder and studio — CLAUDE.md: "BACKLOG.md — Place Rotation (both surfaces; the whole Place rotates as one unit)." A new `rotatable` Guardrail defaults CLOSED (matching `resizable`'s convention); Theme Author authors `holder.rotation` base, Story Author overrides via Selection Action Strip's Place quick-edit (gated on the Guardrail); both engines rotate the whole Place chrome + Place-hosted Layers together around the Place's own centre.
- [x] Draw your own functionality in builder should be same as draw your own feature in studio — CLAUDE.md: "BACKLOG.md — Bug 2: Draw-Your-Own Parity, Studio's Paint-Inside Stack Ported Into World Builder v2." Studio's Shape Paint-Inside stack (Fill Mode toggle, Paint pad with Crayon/Pastel/Brush/Spray media, swatch palettes, thickness tiles, Undo/Clear) ported into Builder v2's Graphics Experience Shape editor via additive `graphicFillMode` + `graphicPaintStrokes` fields, mirrored through `_syncPartLayer` → `convergeSceneLayer` into the compiled `decoration.fillMode`/`decoration.paintStrokes` on the `.vtheme` package. Verified 36/36 (`wb_paint_inside_verify.js`) + Studio sibling 30/30 (`shapes_paint_inside_verify.js`).
- [x] Frame style comes over the art — CLAUDE.md: "BACKLOG.md — Bug: Frame Style Image Ornament No Longer Overpaints the Interior Artwork (both engines)." A full-cover Frame Style = Image ornament used to draw across the interior artwork region as well as the frame band; both engines now clip the ornament image to the frame BAND only via an evenodd (outer-XOR-inner) clip, mirroring `_ornamentWooden`'s established band-only technique. Verified with a targeted 9-interior-point sample.
- [x] Reordering z axis within experience — CLAUDE.md: "BACKLOG.md — Reordering Z Axis Within Experience: Part-Level Reorder (World Builder v2)." A new `ProjectModel.moveExperiencePart(project,experienceId,partId,direction)` swaps both `experience.properties.parts[]` order AND — on every attached Scene — the corresponding mirrored Layer entries' order in `scene.stack`, so paint order genuinely changes on both Working View and Runtime Preview (and, via `convergeScene`, in a compiled/published Theme too). Part cards in the Experience Inspector gain ↑/↓ buttons alongside the existing 🗑️ Remove; disabled at array boundaries and gated on read-only. Primary-part re-sync (parts[0] flat-field mirror + contentKind) mirrors `removeExperiencePart`'s own established precedent. Verified 38/38 via `reorder_parts_verify.js`.
- [x] Extend background-remover utility into Studio's upload flow (Image Studio) — CLAUDE.md: "Feature 1 Phase 2 — Studio's PictureStudio is now Image Studio." On every picture upload, PictureStudio spawns the standalone tool's ES module Web Worker in-process (`new Worker(url,{type:'module'})`) against whatever the user has already flipped/rotated (baked via `_bakeSourceForBg`), and the returned buffer becomes a new source image the existing rotate/flip/crop/enhance stack draws from unchanged. Undo BG reverts to the pristine original. Header rebranded from '📸 Picture Studio' to '🖼️ Image Studio'. Public `PictureStudio.open()` API untouched — every one of the 7 existing call sites across `js/app.js`, `js/contextPanel.js`, and `js/pageDesigner.js` works with zero change. (Phase 1 — standalone tool rebrand + Flip/Rotate — shipped earlier under `tools/background-remover/`, verified end-to-end via a real 4-quadrant Playwright test with pixel sampling.)
- [x] Builder: I should be able to duplicate scenes — CLAUDE.md: "BACKLOG.md — Builder Bug Batch: Duplicate Scenes/Experiences/Parts, Collapsible Part Cards, Publishing Check." The Scene Config panel gains "⧉ Duplicate This Scene"; the copy lands right after its source in the strip, and Experience linkage is repaired per lifecycle (Public: real attachment on the copy, mirror adopted in place so later edits reach it; Personal: mirror flattened into a plain layer — visuals kept, no false linkage a one-scene scope could never honour).
- [x] Builder: I should be able to collapse, expand, and duplicate experience elements — same CLAUDE.md entry. Part card headers collapse/expand (chevron, state survives re-renders); a ⧉ duplicate-part header button (fresh id, lands after its source, never index 0, 5-part cap at the model layer); every Gallery/Nursery card gains ⧉ Duplicate — the clone is born Nurturing with zero attachments.
- [x] Builder: during publishing check, it should show me all the assets unused but on Supabase so that I can remove what is not required — same CLAUDE.md entry. The Publish modal's new Publishing Check diffs this Build's own compiled asset keys against what's genuinely stored under the Theme in the Personal Repository (new ThemeRepositoryClient.listAssetPaths/deleteAssets), lists the orphans, and removes them behind a confirm — then re-checks so the fresh Storage stats are the confirmation. Personal-only by design: Official is self-healing (Promote clears its prefix first).
- [x] Studio: the "🎁 From This World" tile in Add Something should always be there — just grey it out when empty — CLAUDE.md: "BACKLOG.md — From This World Tile Always Present, Greyed When Empty." The tile is now always pushed with a generic disabled card state (muted .is-coming-soon grey, no "Soon" pill, click-inert) when the active World has zero Creator-flagged Collection assets.
- [x] Studio scene is not same as what was authored in builder — `places shapes` half — CLAUDE.md: "BACKLOG.md — Studio-vs-Builder Rendering Divergence: Multi-Stroke Custom Shapes + Transparent Fill Toggle Now Reach Studio (both engines, kept in lockstep)." `builder.js`'s Shape compile branch now carries `customStrokes` (multi-stroke Line/Circle Tool silhouette) + `fillEnabled` (Transparent Fill toggle) additively — both were silently dropped at compile time. `slideRenderer.js`'s `_layerDrawShape` custom branch now checks `d.customStrokes` FIRST (dispatching to the existing `_buildCustomStrokePath` helper), then falls through to `d.customPath`, then to the ellipse placeholder — previously any Custom shape carrying `customStrokes` but no `customPath` silently rendered as an inscribed ellipse. Verified 18/18 via `bug_shape_render_divergence_verify.js` + full regression (both goldenBuilds PASS, museum_realfile_pixelhash byte-identical). `places placeholder art` + `background transparency` halves deferred pending concrete reproduction.
- [x] Wherever we have replace image or replace photo or replace art it should be able to replace from local as well as family photos — CLAUDE.md: "Family Photos at Every Replace-Image Surface." The World-owned object quick-edit's Replace Image, Background's Upload/Replace Picture, and pageDesigner's Cover/Hook/End Add/Replace Image all gain a "📷 From Family Album" button beside the local picker (Creator-gated, hidden when unconfigured), each routing the family pick through the identical pipeline its own local pick already uses.
- [x] All images irrespective from where they are added need to pass through Image Studio before showing up on scene — CLAUDE.md: "Every Image Passes Through Image Studio Before Landing on the Scene." A full entry-point sweep found two offenders (the World-owned quick-edit Replace Image — local AND family — and cardDesigner's Frame "Your Own Picture" chip); both now route through Picture Studio before applying, every other surface confirmed already compliant.
- [x] Voice (direct request — the last Coming Soon tile becomes real) — CLAUDE.md: "Voice MVP Ship 1 — Per-Page Narration (Record + Import, Creator-Only, 60s Cap)." A Creator records or imports a voice narration for the current page (real MediaRecorder, 60s auto-stop cap, preview-before-keep, listen/re-record/remove); a Traveller sees the companion-framed gate. Scoping locked by the product owner: per-page narration FIRST — voice-note OBJECTS on the canvas are the agreed follow-up ship (Ship 2, not started).
- [x] Voice Ship 2 (voice-note objects on the canvas — "so first add voice object") — CLAUDE.md: "Voice MVP Ship 2 — Voice-Note Objects on the Canvas (🔊 Badges, Tap-to-Play)." Placeable 🔊 voice bubbles, several per page, record or import each, a clean tap on the badge plays it right on the canvas; ordinary stickers underneath so move/duplicate/delete/Object Strip all ride the shared machinery. Ship 3 (Story Reel publication) is next.
- [x] Voice Ship 3 (Story Reel publication — "than second lets do story reel publication feture") — CLAUDE.md: "Voice MVP Ship 3 — Story Reel Publication (Narrated Slideshow Video)." The Publish flow's last Coming Soon destination becomes real: every page renders through the same SlideRenderer pipeline, holds for its own narration's real length (a default beat when silent), narrations stitch in as the audio track, and the child downloads a real 1080×1920 webm video — composed entirely client-side via the new js/reelComposer.js (canvas.captureStream + WebAudio + MediaRecorder).

- [x] The exported reel has blank spaces on top and bottom — CLAUDE.md: "Story Reel — Blurred Backdrop Replaces the Flat Letterbox Bars." A 4:5 authored page contains into the 9:16 reel frame by width, leaving ~285px of flat empty bar above and below. The frame is now filled with a softened, scaled-up copy of the page itself, with the sharp page drawn on top casting a soft shadow — every authored pixel stays visible and undistorted (Rule 1: never re-lay-out, stretch, or crop a Scene to reach a platform's frame edges), while the frame reads as composed rather than padded. Additive via opts.backdrop==='blur'; Story Book and Story Carousel composite byte-identically.
- [x] Lets add page turning animations also — CLAUDE.md: "Story Reel — Page-Turn Transitions Between Scenes." A storybook leaf sweeps right-to-left off the spine, bowing as it goes, darkening toward the fold, catching light along its curved free edge, and casting a shadow onto the page it uncovers — a 2D illusion sold entirely by the light, since canvas 2D has no perspective. Narration and hold time begin only once the turn onto a page has finished. ALSO fixed in the same ship: a real defect that shipped with Story Reel and was found by this feature's own verification (not caused by it) — createMediaStreamDestination()'s audio track reports "live" but delivers no samples when nothing is connected, so a reel where no page carried voice composed to a 0-byte file; the destination is now fed real silence for the whole recording (0 bytes before the fix, 64,739 after, same input).
(Checked-off items move here once shipped, with a one-line pointer to the CLAUDE.md entry.)
- [x] World-own image when marked as movable by author means not just repositioning but also rotation and resizable — CLAUDE.md: "Four Studio Bugs, Shipped Together." Move and Resize already worked at the render layer; the real gap was rotation, in two places at once — `_layerDrawDecorationImage` read only the Theme-Author's compiled `d.rotation` and never a Story-Author `ov.rotation` (so an override had nowhere to land), and the quick-edit Rotation slider was gated on text-kind only (a boundary disclosed at the time precisely because a slider would otherwise have been a dead-end control). Both closed together; the shape branch merges `ov.rotation` alongside the `ov.fillColor` it already merged. Colour stays excluded — a full-bleed fill has no orientation.
- [x] Change new idea story world category to start something new in which a blank page with no world association is started — same CLAUDE.md entry. "More Ideas" becomes "Start Something New" (`blank:true`), the one branch on Screen 1: it skips the World screen entirely and lands in the editor with `theme`/`artworkTheme` both `null`. Deliberately never `applyTheme(null)` — `getTheme(null)` substitutes `DEFAULT_THEME_ID`, so that call would silently install storybook-classic; `applyArtworkTheme(null)` IS a real clear and is used for its side effects.
- [x] The project view shows very big placeholder for any project. its not beautiful — same CLAUDE.md entry. Root cause was a CSS cascade-order bug, not a sizing choice: the grid carried BOTH `creation-flow-grid` and `creation-flow-project-grid`, and the generic class is defined LATER at equal specificity, so its auto-fill/200px rule silently won and the project grid's own 4-column rule was dead code. Generic class dropped; the thumbnail also capped at `clamp(78px,10vh,110px)` instead of an uncapped `aspect-ratio:4/5` slab.
- [x] Image Studio: draw + rotate ("not just draw, draw shapes, add colors to them, draw lines — the complete doodle func. should be there as part of beautifying image") — CLAUDE.md: "Image Studio Gains the Full Draw Your Own Stack — By Reusing Studio's, Not Rebuilding It." Shipped as a REUSE, per the product owner's own framing ("we already have a a draw your own functionality in studio why cant we add that into image studio ?"): `js/cardDesigner.js` exports four already-pure helpers (`DOODLE_MEDIA`, `drawDoodleStroke`, `buildShapeSilhouettePath`, `shapeLetterChar`, `padLetterFont`) and Image Studio calls them with its own coordinate space — none of cardDesigner's own pads/panels change. Freehand in all four media (Crayon/Pastel/Brush/Spray) with the shared colour kit + three thicknesses, plus real shapes with fill/outline placed by drag. The drawing is vector data in image-fractional coordinates, composited by ONE shared painter inside the same transform in both preview and bake — so pan/zoom/rotate/flip/crop all carry it and the two can't disagree. Rotation-only (Task #29's other half) stays as it was — 90° steps.
- [x] Studio: allow creator to name their project — same CLAUDE.md entry. `#bookTitle` — the same element Locked Decision #1 originally hid inside `#story-tab` — relocated into the header and un-hidden, styled as a quiet click-to-type title. Deliberately ONE field, not a second: it is what My Projects labels a card with, and it is also the book title a World may show on the page. Amends Locked Decision #1 (noted there rather than left as a silent contradiction).
