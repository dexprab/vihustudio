{
  "version": "3.4.0",
  "phase": "Repository Architecture Transition",
  "milestone": "Sprint 2 — Asset Externalization",
  "slice": "manifest.thumbnail/.previewImage are now plain relative-path references, never embedded base64 — fixed at the source in builder.js and themeEngine.js's zip-import producer; a new ThemeRegistry.resolveAssetRef() resolves them (and Representation thumbnails) uniformly whether the source is a legacy local import or a Supabase repository's signed URL",
  "build": "0040",
  "buildDate": "2026-07-09",
  "commit": "320442b",
  "environment": "Release"
}
