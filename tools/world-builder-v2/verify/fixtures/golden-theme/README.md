# Golden Test Theme

Fixture Theme Project used only by `tools/world-builder/verify/goldenBuild.js`
to prove the full `Validate → Compile → Import → Register → Render` pipeline
works end to end (TB-4.6). Not an Official Theme — never register this in
`js/themeRegistry.js`.
