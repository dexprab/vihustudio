{
  "version": "3.78.0",
  "phase": "BACKLOG.md bug fix -- 'Frame style comes over the art.' A full-cover Frame Style = Image ornament used to overpaint the interior artwork region as well as the frame band; both engines now clip the ornament image to the frame BAND only via an evenodd (outer-XOR-inner) clip, mirroring _ornamentWooden's established band-only technique.",
  "milestone": "engineRuntime.js: _paintFrameOrnament's image branch rewritten to build a two-path clip (outer rect via insets.marginPx + inner content rect via insets.contentInset) and call ctx.clip('evenodd'), so the ornament image is structurally restricted to the frame band alone. Kept in lockstep by hand with root Studio's own renderer/slideRenderer.js _bandOnlyClip helper -- the two rendering engines share no drawing module, per this codebase's twin-file discipline.",
  "slice": "Verified via bug_frame_over_art_verify.js (6/6) sampling 9 interior points at 70px inset from the Place edge -- all magenta (artwork), 0 green (ornament); regression: frame corner still shows green. feature1_frameimage_verify.js (18/18) and bug4_frame_ornament_image_verify.js (24/24) pass unchanged. Both goldenBuild.js suites PASS unchanged.",
  "build": "0150",
  "buildDate": "2026-08-02",
  "commit": "0af73c5",
  "environment": "Release"
}
