{
  "version": "3.76.0",
  "phase": "Place rotation (both surfaces, BACKLOG.md ship-order task #552): whole-Place rotation as one unit around the Place's own centre -- chrome (border/mat/ornament/caption/Place-1 Layer Packs) AND every Place-hosted Layer rotate together. New rotatable Guardrail (default CLOSED, matching resizable's own convention). Story-Author override via slide.metadata.elementOverrides[placeId].rotation wins over Theme-Author placeRects[i].rotation base -- the same tri-state precedence every other rotation-aware platform field already uses.",
  "milestone": "World Builder v2 side: holder.permissions.rotatable in projectModel.js's Place-permission schema (default CLOSED via _resolvePlacePermissions, matching resizable's own precedent); a 'Can a Story Author rotate this?' checkbox in worldBuilderApp.js's Place permission block; a Theme-Author Rotation slider on the Place editor; services/builder.js's convergeScene carries rotatable + placeRects[i].rotation through to the compiled Layout; services/engineRuntime.js's _paintHolder rotates chrome+content together around the Place's centre via the established save/translate/rotate/translate/restore idiom. Studio side: renderer/slideRenderer.js's _drawPlaceOne wrap at the reorderable-loop entry (lines 3800-3835) rotates the artwork/ornament/caption pipeline; Place 1's mat/fill/panel drawn ONCE outside the loop (lines 3681-3686) also rotates when rotation is present. js/selectionActionStrip.js's Place edit popup gained a Rotation slider gated on the rotatable Guardrail (never shown for a non-rotatable Place), writing through SceneEngine.setContentOverride via the shared _afterPlaceEdit tail so Object Strip/Runtime Preview all live-refresh on every drag tick.",
  "slice": "Verified 26/26 via place_rotation_verify.js: rotatable Guardrail genuinely opts in (non-rotatable Place shows zero slider), a real mouse-driven slider drag persists the override AND the render tree bbox visibly rotates in both engines (differCount 1/36 grid-sample pixel-diff proof against a distinctive 4-quadrant test image -- a uniform-colour fixture cannot distinguish rotation), Theme-Author-authored base rotation renders when no Story-Author override, override wins over base when both present, non-square Place at non-orthogonal 45deg angle prevents the square-lands-on-itself pitfall. Full regression: goldenBuild.js (v1 and v2) PASS, place_resize_shape_verify.js zero failures, place_guardrails_runtime_verify.js 10/0, museum_realfile_pixelhash.js byte-identical hash SHA-256 e7b2a905613dbef3d2f9064211103d84762d7039d3bee404df90f4d90899b390 confirming zero visual/rendering regression. Ship-order after this: #553 Bug 2 Draw-Your-Own parity port (Studio Paint-Inside stack -> Builder v2).",
  "build": "0148",
  "buildDate": "2026-08-01",
  "commit": "0000000",
  "environment": "Release"
}
