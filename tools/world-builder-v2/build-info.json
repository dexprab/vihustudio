{
  "version": "3.72.0",
  "phase": "BACKLOG.md Batch 1 -- Bugs E + D (Graphics rotation reaches Studio + Add Experience in Scene Stack sidebar)",
  "milestone": "Bug E (root cause + Publish Fidelity): projectModel.js's _syncUniversalContent hardcoded rotation:0 for a plain uploaded Graphics image slot ('shapeKind ? ... : 0'), silently discarding the graphicRotation slider's persisted value before it ever reached the mirrored Scene Layer -- while the sibling Shape-kind case correctly wrote graphicRotation through. Fixed at line 1879: read graphicRotation for any graphic slot, image or Shape alike (`isImage ? (props.imageRotation||0) : (props.graphicRotation||0)`). Confirmed via decisive Playwright end-to-end: mirrored Scene Layer's .rotation now === 135 for image (previously 0), Shape-kind rotation === 90 regression still holds, AND the compiled Layer Pack entry (packageData.layerPack, top-level not under .theme, confirmed by real ProjectCompiler diagnostic) carries decoration.rotation:135/90 through to the .vtheme package -- Studio's already-correct renderer receives the honoured rotation, closing Creator Governing Rule #5 (Publish Fidelity). Bug D: '+ Add Experience' button added to the Scene Stack sidebar (worldBuilderApp.js _renderSceneStackTab, after '+ Add a Place'), full 100%-width matching Place, disabled === currentProjectReadOnly matching Place's Guardrails binding exactly (Creator Governing Rule #2). Clicking opens the Nursery tab in Experience Home with the create form pre-open (experienceHomeZone='nursery', experienceCreateFormOpen=true), giving Theme Authors one-click access to new-Experience creation directly from where they compose their Scene. Verified 26/26 assertions passing via batch1_bug_e_d_verify.js: model-layer proof of the rotation fix, Shape-kind regression, Publish Fidelity (compiled decoration.rotation for both image and shape), Guardrails binding source-level check (both Place and Experience buttons bind .disabled = currentProjectReadOnly), UI flow (create button appears/enables/opens Nursery form/successfully creates a real Experience). Both goldenBuild.js suites (World Builder v1 and v2) PASS unchanged. Confined to tools/world-builder-v2/js/projectModel.js (Engine Adapter rotation resolution) and tools/world-builder-v2/js/worldBuilderApp.js (Scene Stack sidebar button).",
  "slice": "Root Studio (js/, renderer/) is untouched by this batch -- Bug E's rotation fix flows through the already-correct root Studio _layerDrawDecorationImage renderer (fixed by an earlier 'BUG: Studio drops rotation on image-kind Decorations' sprint), so the compiled package's own decoration.rotation now reaches its already-correct destination. Bugs 2 (Draw Your Own parity in v2) and 5 (Companion) remain deferred; Bug B (companion blank circle) explicitly dropped per prior scope decision. Next batches per plan: G, then F+A, then H, then C.",
  "build": "0144",
  "buildDate": "2026-08-01",
  "commit": "PENDING",
  "environment": "Release"
}
