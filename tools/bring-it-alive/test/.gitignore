# The camera-feed fixture is uncompressed YUV (~3.7 MB), generated on demand
# from tools/imagebed/1000299474.jpg by make-camera-feed.js — never committed.
camera-feed-001.y4m

# The handwriting camera fixtures are uncompressed YUV, generated on demand
# from the seeded letter composer by make-hw-feeds.js — never committed.
# hw-feeds.json carries their geometry and version stamp. (hw-line-*.y4m,
# hw-card-*.y4m and the sheet-era names belong to earlier eras; kept ignored
# so a stale checkout never offers one to git.)
hw-*.y4m
hw-feeds.json
