{
  "version": "2.181.1",
  "sprint": "Every image now passes through Image Studio before it lands on the scene — a standing product rule stated directly ('all images irrespective from where they are added they need to pass through image studio before showing up on scene'). A full sweep of every image entry point found exactly two that skipped it: (1) the World-owned object quick-edit popup's Replace Image — BOTH its local file path and the just-shipped Family Photos path applied directly (the prior ship's 'parity with local' reasoning was matching the wrong baseline: the local path was itself the gap) — both now route through a shared prepImage() into PictureStudio.open('fit'); (2) cardDesigner's Frame Style 'Your Own Picture' chip — a raw FileReader upload — now routes through PictureStudio.open('fill', matching the frame band's cover-fit rendering). Every other surface (Artwork Place, Background, pageDesigner Image Manager, Add Something Photo, Family freestanding/artwork picks) was confirmed already routing through Image Studio; the only remaining raw FileReader sites are the documented degraded fallbacks for the theoretical no-PictureStudio case.",
  "task": "js/contextPanel.js: quick-edit image branch gains prepImage() (Picture Studio 'fit') used by both local + family sources. js/cardDesigner.js: frame chip routes through Picture Studio 'fill' before _storeUploadedAsset. Verified 22/22 (fa_replace_everywhere_verify.mjs — T1 rewritten to assert the Image Studio pass + no premature apply, new T5 covering the frame chip end to end via a real filechooser) + fa_ui_verify 35/35 + photo_via_studio 8/8 + both goldenBuilds PASS. Cache-bust ?v=0334 -> ?v=0335.",
  "build": "0330",
  "buildDate": "2026-08-05",
  "commit": "904ed0462bffdeada32383c4c66571ed61e11de9",
  "environment": "Release"
}
