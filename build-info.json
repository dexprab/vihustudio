{
  "version": "2.125.0",
  "sprint": "Lumo Voice — Real Recordings Swap In",
  "task": "'ok i have recorded all the prompts. same file name prefixed with v.' Every one of Lumo's 21 spoken lines (tap-grid Gatekeeper greeting, Traveller Gateway greeting pairs, Returning Creator recognition + reunion pairs, Creator Ceremony's two Guardian beats, sky-recognition challenge's 4 lines) was re-recorded and delivered as vlumo-NN-<name>.mp3 files uploaded to the repo. Each was git mv'd directly over its matching placeholder -- every filename js/lumoVoice.js's LINES table already points at is unchanged, only the bytes at each path changed. Every clip's real duration was re-measured via ffprobe and the ms value for all 21 LINES entries updated to match (several differ from the old placeholder timing by a full second or more), so on-screen bubble hold times (js/gatewaySequence.js's playLines(), which stretches a beat to fit LumoVoice.durationMs()) now genuinely track the real spoken length. lumo-22-flying.mp3/lumo-23-landing.mp3 (non-verbal) were explicitly left untouched per direct instruction. Verified via a new Playwright suite confirming all 23 durationMs() values match, preload()/play()/playSequence() run with zero throw, and zero unexpected console errors, plus a git-stash A/B confirming two pre-existing test-script quirks (sky_voice_test.js's own flaky outcome + network-block noise) reproduce identically on the unmodified baseline. No Theme Contract/Engine V2 Scene Model/Builder/Creator Governing Rules change, no JS logic change beyond duration constants; confined to js/lumoVoice.js and the 21 audio files under assets/lumo/voice/.",
  "build": "0267",
  "buildDate": "2026-07-26",
  "commit": "pending",
  "environment": "Release"
}
