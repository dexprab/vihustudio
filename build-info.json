{
  "version": "2.184.0",
  "sprint": "Voice MVP Ship 3 — Story Reel publication (the third slice of Locked Product Decision #4, and the direct answer to 'how can a narrated book be published'). The Publish flow's last Coming Soon destination becomes real: a narrated slideshow VIDEO — every page renders through the same SlideRenderer pipeline the editor/Book/Carousel already use (Rule 5), holds on screen for its own narration's real decoded length (a default beat when silent), and the narrations stitch in as the audio track. The child downloads a real 1080×1920 webm they can post as an Instagram Reel.",
  "task": "New js/reelComposer.js — a pure composition engine (voiceRecorder.js discipline: module = engine, caller = UI): canvas.captureStream(fps) + a rAF repaint loop (a static canvas may stop emitting frames) for video, the codebase's FIRST AudioContext + createMediaStreamDestination() for audio (each page's narration an AudioBufferSourceNode started as its page appears), MediaRecorder stitching both into webm; records in REAL TIME. js/storyDestinations.js: REEL's comingSoon stub replaced with real hooks — encodePage resolves a page's narration via AssetStore (recalled-owner fallback) and decodes it to an AudioBuffer whose real duration drives the page hold; finish() composes and names <title>_reel.webm; optional publishMessages/finishingMessage/progressNoun presentation fields (additive contract). js/publishStudio.js: the render loop and finalize now await thenable hook results (Book/Carousel byte-identical), ctx carries the slide (additive), destination-aware Publishing copy ('Filming your story…', 'Scene X of Y') + reel Celebration copy ('Your story is now a movie!', 'Download My Reel'); the coming-soon banner genericized since no built-in destination is Coming Soon anymore. Verified 15/15 (reel_verify.mjs — real end-to-end publish, EBML magic bytes checked on the actual downloaded blob) + voice_note 22/22 + voice 16/16 + fromworld 13/13 + fa_ui 35/35 + photo_via_studio 8/8 + both goldenBuilds PASS. Cache-bust ?v=0337 -> ?v=0338 (54 tags — reelComposer.js joins).",
  "build": "0333",
  "buildDate": "2026-08-05",
  "commit": "PENDING",
  "environment": "Release"
}
