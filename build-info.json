{
  "version": "2.180.4",
  "sprint": "BACKLOG bug — the Add Something 'From This World' tile is now ALWAYS present, greyed out (disabled, no click, no 'Soon' pill) when the active World has zero Creator-flagged Collection assets — per direct product decision ('the tile should always be there. just grey it out when there is nothing to show'), reversing the earlier hide-entirely behaviour which read as the tile having vanished.",
  "task": "js/contextPanel.js: fromWorld item always pushed with disabled:_activeCollectionAssets().length===0; the card builder gained a generic disabled state (reuses .is-coming-soon grey, no Soon pill). Cache-bust ?v=0331 -> ?v=0332.",
  "build": "0327",
  "buildDate": "2026-08-05",
  "commit": "PENDING",
  "environment": "Release"
}
