{
  "version": "2.180.0",
  "sprint": "Family Photos — the album picker and Manage Albums now open in a real popup modal instead of the narrow right-sidebar strip. A wide (min(880px,94vw)) centered dialog with a multi-column photo grid (repeat(auto-fill,minmax(150px,1fr))) shows many photos at a glance instead of one-per-scroll; header carries a ✕ close, backdrop-click and Escape both close, and picking a photo closes the popup BEFORE Picture Studio opens so the two modals never stack. The right panel behind is never touched — closing lands the kid exactly where they were in Personalize.",
  "task": "js/contextPanel.js: new _ensureFamilyModal/_openFamilyModal/_closeFamilyModal machinery; _showFamilyPhotosPicker and _showFamilyAlbumManager both render into the modal body; zero panelRoot writes remain in the family section. css/style.css: .family-photos-modal/-box/-header/-title/-close/-body/-grid block. Cache-bust ?v=0327 -> ?v=0328.",
  "build": "0323",
  "buildDate": "2026-08-05",
  "commit": "PENDING",
  "environment": "Release"
}
