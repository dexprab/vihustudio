{
  "version": "2.180.1",
  "sprint": "Add Something → Photo now routes through Image Studio. The reported bug ('when i choose image from +add something it does not goes through image studio') was real: the Photo card's handler read the picked file via FileReader and created the sticker directly, skipping Picture Studio entirely — unlike every other picture path (Place fills, Family Photos picks, page background). The picked file now opens the same Picture Studio pass first; applying creates the freestanding kind:'image' sticker exactly as before.",
  "task": "js/contextPanel.js: _addImageObject routes through PictureStudio.open(file,{defaultMode:'fit',...}); shared sticker tail factored into _addImageStickerFromDataURL (reused by _familyAddImageObject). Cache-bust ?v=0328 -> ?v=0329.",
  "build": "0324",
  "buildDate": "2026-08-05",
  "commit": "8b2f20f83b87896d65781b507c09038e022b57c5",
  "environment": "Release"
}
