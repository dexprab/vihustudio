{
  "version": "2.162.0",
  "sprint": "Bug G Rework -- Curve Style picker (None/Arc/Wave/Circle) + gated Amount slider replacing the old plain single Curve slider, end to end across all 3 authoring surfaces + both rendering engines",
  "task": "Reworks the just-shipped Bug G (Curve text) per an approved product refinement: replaces the single Curve amount slider with a Curve Style picker (None / Arc / Wave / Circle) paired with an Amount slider that's DISABLED when style is None or Circle (Circle deliberately ignores amount, wrapping a full 360 degrees at radius = textWidth / (2*pi)). Every Text object across World Builder v2 + Story-owned freeform + World-owned quick-edit now supports all four styles symmetrically. Data model: adds textCurveStyle:'none' default alongside existing textCurve:0 in experienceSchema.js defaults + PART_FIELD_KEYS.text. Engine Adapter (_syncPartLayer text branch) writes curveStyle onto the mirrored Scene Layer's .curveStyle field alongside .curve. Publish Fidelity (Rule #5): builder.js's text compile branch carries curveStyle onto compiled text.curveStyle. Both engines lockstep: engineRuntime.js + renderer/slideRenderer.js each expose _drawStyledTextLine dispatcher routing to _drawCurvedTextLine (arc, per-char radial layout), _drawWaveTextLine (sine, amplitude=|amount|*0.3, sign flips direction, glyphs stay upright), or _drawCircleTextLine (full 360 wrap, glyphs rotate tangentially). Bbox expansion per style: arc sagitta, wave amplitude, circle 2*radius square. Shape-Mosaic Fill Style now dispatched through shared drawStyledLine helper so curve applies to it too (previously silently skipped). _drawFreeformText (Story-owned) rewritten in this session for curveStyle-aware rendering; _layerDrawText (World-owned) rewritten in the prior turn. UI: worldBuilderApp.js Text Inspector exposes TEXT_CURVE_STYLE_CHOICES select paired with existing Amount via _fieldRow; both js/contextPanel.js quick-edit popups got identical Style picker + gated Amount via _makeIconChoiceRow + syncAmountDisabled. Verified 44/44 assertions via bug_g_rework_verify.js: P1 (schema defaults + PART_FIELD_KEYS), P1b (Adapter mirrors curveStyle+curve), P2 (real ProjectCompiler.runBuild compiles text.curveStyle+text.curve to .vtheme), P3 (both engines' source-level dispatcher/helpers), P4 (backward-compat two-layer resolution honours explicit None via caller-side isCurvedActive gate even with nonzero curve), P5 (distinct pixel signatures in Studio Reader-facing renderer: none flat 38px, arc bows 63px, wave undulates 73px, circle wraps 5 samples, legacy matches arc, arc vs wave differ by 10px), P6 (all 3 authoring surfaces have Style picker + amount-disable gate), P7 (Shape-Mosaic dispatched through drawStyledLine in both text renderers). Both goldenBuild.js suites (World Builder v1 and v2) PASS unchanged. Ship-order committed: Bug G Rework -> Bug 4 (Image in Frame Styles) -> Add more Places -> Place rotation -> Bug 2 (Draw-Your-Own parity).",
  "build": "0304",
  "buildDate": "2026-08-01",
  "commit": "bc18c7fdd9aa03cc17c66f45b7b79c5d46b1ccbf",
  "environment": "Release"
}
