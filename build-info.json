{
  "version": "2.180.5",
  "sprint": "Shared repository client gains listAssetPaths/deleteAssets (js/themeRepositoryClient.js) — the two additive functions World Builder v2's new Publishing Check needs to diff/remove a Personal Theme's unused-but-on-Supabase Storage assets. Both build the canonical {repo}/{ownerSegment}/{themeId} prefix; listAssetPaths reuses the recursive _listAllObjectPaths and returns bare relPaths; deleteAssets removes re-prefixed full paths from the theme-assets bucket, with an empty-array clean early return. Zero root-Studio behaviour change — purely additive exports.",
  "task": "See tools/world-builder-v2/build-info.json 3.81.0 for the full three-bug Builder batch this serves. Cache-bust ?v=0332 -> ?v=0333.",
  "build": "0328",
  "buildDate": "2026-08-05",
  "commit": "PENDING",
  "environment": "Release"
}
